import { createFileRoute } from "@tanstack/react-router";import { ProtectedRoute } from "@/components/protected-route";import { DocumentRegistryReviewsWorkspace } from "@/modules/document-registry";
export const Route=createFileRoute("/modules/document-registry/reviews")({component:()=> <ProtectedRoute requiredModule="document_registry" requiredPermission="document_registry:dispose" requiredRecordScope="document_registry.records" requiredRecordScopeKind="campus"><DocumentRegistryReviewsWorkspace/></ProtectedRoute>});
