import { createFileRoute } from "@tanstack/react-router";import { ProtectedRoute } from "@/components/protected-route";import { DocumentRegistryRetentionWorkspace } from "@/modules/document-registry";
export const Route=createFileRoute("/modules/document-registry/retention")({component:()=> <ProtectedRoute requiredModule="document_registry" requiredPermission="document_registry:dispose"><DocumentRegistryRetentionWorkspace/></ProtectedRoute>});
